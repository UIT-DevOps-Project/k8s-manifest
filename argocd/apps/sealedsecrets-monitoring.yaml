apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sealedsecrets-monitoring
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/UIT-DevOps-Project/k8s-manifest
    targetRevision: main
    path: sealedsecrets/monitoring
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
