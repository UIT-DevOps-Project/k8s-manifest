apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sealedsecrets-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/UIT-DevOps-Project/k8s-manifest
    targetRevision: main
    path: apps/sealedsecrets/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true